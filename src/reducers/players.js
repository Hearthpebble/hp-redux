/* eslint-env node*/

const initialState = {
  byId: {},
};

const players = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

module.exports = players;
