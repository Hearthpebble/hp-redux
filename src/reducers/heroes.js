/* eslint-env node*/

const initialState = {
  byId: {},
};

const heroes = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

module.exports = heroes;
